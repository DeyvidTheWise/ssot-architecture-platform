"use client";
import { StubPage } from "@/components/ui/stub-page";

export default function DiagramsPage() {
  return <StubPage title="Diagrams" description="Mermaid / UML / ERD editor with live preview. GET/POST /api/projects/:id/diagrams." />;
}
