"use client";
import { StubPage } from "@/components/ui/stub-page";

export default function ExportPage() {
  return <StubPage title="Export SSOT" description="Bundle artifacts, graph, validation report and version history as JSON / Markdown / PDF / ZIP. POST /api/projects/:id/export." />;
}
