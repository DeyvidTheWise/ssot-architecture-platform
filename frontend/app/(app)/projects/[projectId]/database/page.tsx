"use client";
import { StubPage } from "@/components/ui/stub-page";

export default function DatabasePage() {
  return <StubPage title="Database model" description="Entities, fields, primary and foreign keys. GET /api/database-models/:id/entities." />;
}
