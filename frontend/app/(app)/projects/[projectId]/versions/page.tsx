"use client";
import { StubPage } from "@/components/ui/stub-page";

export default function VersionsPage() {
  return <StubPage title="Version history" description="Timeline of every change. GET /api/projects/:id/versions." />;
}
